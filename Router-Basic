// npm install express --save
//nodemon 
const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Welcome');
});

app.get('/about', (req, res) => {
  res.send('About page!')
});

app.listen(3000);
